F2A

string hexString = create2faAccountAnQrCode(username, userNUmber)

boolean validate2faAccount(username, token)

- Get Master Public Key:

	    List<PGPPublicKey> publicKeys
	    PgpKeyPairTransfer pgpKeyTransfer = new PgpKeyPairTransfer(connection,
		    ServerParms.getMasterKeyUserNumber());

	    PgpKeyPairLocal pgpKeyPairLocal = pgpKeyTransfer.get();
	    PgeepPublicKey pgeepPublicKey = pgpKeyPairLocal.getPgeepPublicKey();
	    publicKeys.add(pgeepPublicKey.getKey());
	    
- Encrypt base secret key:
		pgpActions.encryptStringPgp(subject, publicKeys);
	
- Decrypt base secret key:
		passphrase = ServletParms.getMasterKeyPassphrase();	
		PGPPrivateKey privateKey = keyHandler.getPgpSecretKeyFromAsc(keyBloc, oldBody, passphrase);
		String body = pgpActions.decryptStringPgp(oldBody, privateKey, null);
	
	

	


